import 'package:flutter/material.dart';

class PokemonImagePage extends StatelessWidget {
  const PokemonImagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PokemonImage'),
      ),
    );
  }
}
